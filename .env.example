# Base URL of the Nomalyze Django backend (no trailing slash).
# Local development default — override per environment.
# Production: set this in your host's environment (e.g. Render env vars or a .env.production file).
VITE_API_BASE_URL=http://localhost:8000
